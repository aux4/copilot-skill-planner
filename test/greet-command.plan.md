# Plan: greet-command

## Task
Create a simple greet command that prints "hello world".

## Context
- No existing `.aux4` file in the current directory
- Need to create a new aux4 configuration from scratch
- The greet command will be a simple command in the `main` profile that uses `echo` to print "hello world"

## Steps
1. [ ] Create .aux4 configuration file with greet command that prints hello world
